import { FilterButton } from 'components';
import * as Styled from './styled';

export const FilterList = () => (
  <Styled.Container>
    <FilterButton label="Filter 1" />
    <FilterButton label="Filter 2" />
  </Styled.Container>
);
